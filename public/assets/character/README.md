# Character asset hold

No production Fındık character art belongs in this directory until the original supplied sticker is added as the approved reference.

The current app uses an explicit illustrated placeholder silhouette and labels it as such. Once the source sticker is available, add the approved source and update `docs/ASSET_MANIFEST.md` before replacing the placeholder.
