# Supabase project settings. The app remains local-first without these values.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Comma-separated exact browser origins allowed for magic-link redirects.
NEXT_PUBLIC_AUTH_REDIRECT_ORIGINS=http://localhost:3000
